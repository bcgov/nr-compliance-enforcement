<!-- SOURCE NAME triage report. Rendered via tools/n8n/render.sh from the merged
     collection JSON (the chained collection snippets). Cleaned from SOURCE NAME.
     Inputs (the keys this template expects; list env keys bare so render.sh leaves them):
       VAR               env / arg            — describe it
       .json.path        from snippet-X       — describe it
       .other[]          from snippet-Y       — describe it
-->

# TITLE — ${NAMESPACE}

## Section

<!-- Fill only from the Inputs above: {{ .json.path }} (jq) and/or ${VAR} (env). -->
