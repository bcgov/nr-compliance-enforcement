generator client {
  provider        = "prisma-client-js"
  output          = "../../node_modules/.prisma/investigation"
  previewFeatures = ["multiSchema"]
  binaryTargets   = ["native", "debian-openssl-1.1.x"]
}

generator prismaClassGenerator {
  provider = "prisma-class-generator"
  output   = "./generated"
  dryRun   = "false"
}

datasource db {
  provider = "postgresql"
  url      = env("INVESTIGATION_POSTGRES_URL")
  schemas  = ["investigation"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model activity_note {
  activity_note_guid                                                      String             @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_guid                                                      String             @db.Uuid
  content_json                                                            Json
  content_text                                                            String?
  actioned_app_user_guid_ref                                              String?            @db.Uuid
  reported_utc_timestamp                                                  DateTime?          @db.Timestamp(6)
  reported_app_user_guid_ref                                              String?            @db.Uuid
  active_ind                                                              Boolean            @default(true)
  create_user_id                                                          String             @db.VarChar(32)
  create_utc_timestamp                                                    DateTime           @default(now()) @db.Timestamp(6)
  update_user_id                                                          String?            @db.VarChar(32)
  update_utc_timestamp                                                    DateTime?          @db.Timestamp(6)
  task_guid                                                               String?            @db.Uuid
  activity_note_code                                                      String             @db.VarChar(16)
  actioned_utc_date                                                       DateTime?          @db.Date
  actioned_utc_time                                                       DateTime?          @db.Time(6)
  activity_note_code_activity_note_activity_note_codeToactivity_note_code activity_note_code @relation("activity_note_activity_note_codeToactivity_note_code", fields: [activity_note_code], references: [activity_note_code], onDelete: NoAction, onUpdate: NoAction, map: "fk_activity_note_code")
  task                                                                    task?              @relation(fields: [task_guid], references: [task_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_activity_note_task")
  investigation                                                           investigation      @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_investigation_guid")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model activity_note_code {
  activity_note_code                                                 String          @id @db.VarChar(16)
  short_description                                                  String          @db.VarChar(64)
  long_description                                                   String?         @db.VarChar(256)
  display_order                                                      Int?
  active_ind                                                         Boolean         @default(true)
  create_user_id                                                     String          @db.VarChar(32)
  create_utc_timestamp                                               DateTime        @default(now()) @db.Timestamp(6)
  update_user_id                                                     String?         @db.VarChar(32)
  update_utc_timestamp                                               DateTime?       @db.Timestamp(6)
  activity_note_activity_note_activity_note_codeToactivity_note_code activity_note[] @relation("activity_note_activity_note_codeToactivity_note_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contravention {
  contravention_guid             String                     @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_guid             String?                    @db.Uuid
  legislation_guid_ref           String                     @db.Uuid
  active_ind                     Boolean                    @default(true)
  create_user_id                 String                     @db.VarChar(32)
  create_utc_timestamp           DateTime                   @default(now()) @db.Timestamp(6)
  update_user_id                 String?                    @db.VarChar(32)
  update_utc_timestamp           DateTime?                  @db.Timestamp(6)
  contravention_date             DateTime?                  @db.Date
  geo_organization_unit_code_ref String?                    @db.VarChar(10)
  investigation                  investigation?             @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction)
  contravention_party_xref       contravention_party_xref[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contravention_party_xref {
  contravention_party_xref_guid String               @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  contravention_guid            String               @db.Uuid
  investigation_party_guid      String?              @db.Uuid
  active_ind                    Boolean              @default(true)
  create_user_id                String               @db.VarChar(32)
  create_utc_timestamp          DateTime             @default(now()) @db.Timestamp(6)
  update_user_id                String?              @db.VarChar(32)
  update_utc_timestamp          DateTime?            @db.Timestamp(6)
  contravention                 contravention        @relation(fields: [contravention_guid], references: [contravention_guid], onDelete: NoAction, onUpdate: NoAction)
  investigation_party           investigation_party? @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction)
  enforcement_action            enforcement_action[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model diary_date {
  diary_date_guid          String        @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_guid       String        @db.Uuid
  due_date                 DateTime      @db.Date
  description              String        @db.VarChar(4000)
  active_ind               Boolean       @default(true)
  create_user_id           String        @db.VarChar(32)
  app_create_utc_timestamp DateTime      @db.Timestamp(6)
  app_create_user_guid_ref String?       @db.Uuid
  app_update_utc_timestamp DateTime?     @db.Timestamp(6)
  app_update_user_guid_ref String?       @db.Uuid
  create_utc_timestamp     DateTime      @default(now()) @db.Timestamp(6)
  update_user_id           String?       @db.VarChar(32)
  update_utc_timestamp     DateTime?     @db.Timestamp(6)
  task_guid                String?       @db.Uuid
  investigation            investigation @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_diary_date_investigation")
  task                     task?         @relation(fields: [task_guid], references: [task_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_diary_date_task")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model enforcement_action {
  enforcement_action_guid                                                                     String                   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  contravention_party_xref_guid                                                               String                   @db.Uuid
  enforcement_action_code                                                                     String                   @db.VarChar(16)
  date_issued                                                                                 DateTime                 @db.Date
  geo_organization_unit_code_ref                                                              String                   @db.VarChar(16)
  app_user_guid_ref                                                                           String                   @db.Uuid
  active_ind                                                                                  Boolean                  @default(true)
  create_user_id                                                                              String                   @db.VarChar(32)
  create_utc_timestamp                                                                        DateTime                 @default(now()) @db.Timestamp(6)
  update_user_id                                                                              String?                  @db.VarChar(32)
  update_utc_timestamp                                                                        DateTime?                @db.Timestamp(6)
  contravention_party_xref                                                                    contravention_party_xref @relation(fields: [contravention_party_xref_guid], references: [contravention_party_xref_guid], onDelete: NoAction, onUpdate: NoAction)
  enforcement_action_code_enforcement_action_enforcement_action_codeToenforcement_action_code enforcement_action_code  @relation("enforcement_action_enforcement_action_codeToenforcement_action_code", fields: [enforcement_action_code], references: [enforcement_action_code], onDelete: NoAction, onUpdate: NoAction)
  ticket                                                                                      ticket[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model enforcement_action_code {
  enforcement_action_code                                                                                                  String                                @id @db.VarChar(16)
  short_description                                                                                                        String                                @db.VarChar(64)
  long_description                                                                                                         String?                               @db.VarChar(256)
  display_order                                                                                                            Int?
  active_ind                                                                                                               Boolean                               @default(true)
  create_user_id                                                                                                           String                                @db.VarChar(32)
  create_utc_timestamp                                                                                                     DateTime                              @default(now()) @db.Timestamp(6)
  update_user_id                                                                                                           String?                               @db.VarChar(32)
  update_utc_timestamp                                                                                                     DateTime?                             @db.Timestamp(6)
  enforcement_action_enforcement_action_enforcement_action_codeToenforcement_action_code                                   enforcement_action[]                  @relation("enforcement_action_enforcement_action_codeToenforcement_action_code")
  enforcement_action_code_agency_xref_enforcement_action_code_agency_xref_enforcement_action_codeToenforcement_action_code enforcement_action_code_agency_xref[] @relation("enforcement_action_code_agency_xref_enforcement_action_codeToenforcement_action_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model enforcement_action_code_agency_xref {
  enforcement_action_code_agency_xref_guid                                                                     String                  @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  enforcement_action_code                                                                                      String                  @db.VarChar(16)
  agency_code_ref                                                                                              String                  @db.VarChar(16)
  active_ind                                                                                                   Boolean                 @default(true)
  create_user_id                                                                                               String                  @db.VarChar(32)
  create_utc_timestamp                                                                                         DateTime                @default(now()) @db.Timestamp(6)
  update_user_id                                                                                               String?                 @db.VarChar(32)
  update_utc_timestamp                                                                                         DateTime?               @db.Timestamp(6)
  enforcement_action_code_enforcement_action_code_agency_xref_enforcement_action_codeToenforcement_action_code enforcement_action_code @relation("enforcement_action_code_agency_xref_enforcement_action_codeToenforcement_action_code", fields: [enforcement_action_code], references: [enforcement_action_code], onDelete: NoAction, onUpdate: NoAction, map: "enforcement_action_code_agency_xre_enforcement_action_code_fkey")

  @@unique([enforcement_action_code, agency_code_ref], map: "uq_enforcement_action_code_agency")
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model enforcement_action_h {
  h_enforcement_action_guid     String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model exhibit {
  exhibit_guid                   String        @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  task_guid                      String        @db.Uuid
  investigation_guid             String        @db.Uuid
  exhibit_number                 Int
  exhibit_display_number         String        @db.VarChar(64)
  property_type                  String        @db.Char(1)
  description_text               String
  quantity_amount                Int?
  seized_from_first_name         String?       @db.VarChar(128)
  seized_from_last_name          String?       @db.VarChar(128)
  seized_from_address            String?       @db.VarChar(256)
  seized_from_phone_number       String?       @db.VarChar(16)
  collected_utc_timestamp        DateTime      @default(now()) @db.Timestamp(6)
  collected_by_app_user_guid_ref String        @db.Uuid
  location_of_intake_text        String?
  property_tag_number            String        @db.VarChar(64)
  active_ind                     Boolean       @default(true)
  create_user_id                 String        @db.VarChar(32)
  create_utc_timestamp           DateTime      @default(now()) @db.Timestamp(6)
  update_user_id                 String?       @db.VarChar(32)
  update_utc_timestamp           DateTime?     @db.Timestamp(6)
  investigation                  investigation @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_exhibit__investigation")
  task                           task          @relation(fields: [task_guid], references: [task_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_exhibit__task")

  @@unique([task_guid, exhibit_number], map: "uq_exhibit_number_per_task")
  @@schema("investigation")
}

model flyway_schema_history {
  installed_rank Int      @id(map: "flyway_schema_history_pk")
  version        String?  @db.VarChar(50)
  description    String   @db.VarChar(200)
  type           String   @db.VarChar(20)
  script         String   @db.VarChar(1000)
  checksum       Int?
  installed_by   String   @db.VarChar(100)
  installed_on   DateTime @default(now()) @db.Timestamp(6)
  execution_time Int
  success        Boolean

  @@index([success], map: "flyway_schema_history_s_idx")
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an index with non-default null sort order and requires additional setup for migrations. Visit https://pris.ly/d/default-index-null-ordering for more info.
model investigation {
  investigation_guid                 String                       @id(map: "PK_investigation_guid") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_description          String?                      @db.VarChar(4000)
  owned_by_agency_ref                String                       @db.VarChar(10)
  investigation_status               String                       @db.VarChar(10)
  investigation_opened_utc_timestamp DateTime                     @db.Timestamp(6)
  create_user_id                     String                       @db.VarChar(32)
  create_utc_timestamp               DateTime                     @db.Timestamp(6)
  update_user_id                     String?                      @db.VarChar(32)
  update_utc_timestamp               DateTime?                    @db.Timestamp(6)
  name                               String                       @default("") @db.VarChar(100)
  location_geometry_point            Unsupported("geometry")?
  location_address                   String?                      @db.VarChar(120)
  location_description               String?                      @db.VarChar(4000)
  created_by_app_user_guid_ref       String?                      @db.Uuid
  supervisor_guid_ref                String?                      @db.Uuid
  primary_investigator_guid_ref      String?                      @db.Uuid
  file_coordinator_guid_ref          String?                      @db.Uuid
  discovery_date_utc_date            DateTime?                    @db.Date
  discovery_date_utc_time            DateTime?                    @db.Time(6)
  geo_organization_unit_code_ref     String?                      @db.VarChar(10)
  activity_note                      activity_note[]
  contravention                      contravention[]
  diary_date                         diary_date[]
  exhibit                            exhibit[]
  investigation_status_code          investigation_status_code    @relation(fields: [investigation_status], references: [investigation_status_code], onDelete: NoAction, onUpdate: NoAction, map: "FK_investigation__investigation_status")
  investigation_party                investigation_party[]
  officer_investigation_xref         officer_investigation_xref[]
  task                               task[]

  @@unique([name, owned_by_agency_ref], map: "uq_investigation_name_agency")
  @@index([location_geometry_point], map: "idx_investigation_location_geometry_point", type: Gist)
  @@index([owned_by_agency_ref, investigation_status, investigation_opened_utc_timestamp(sort: Desc)], map: "idx_investigation_agency_status_opened")
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_alias {
  investigation_alias_guid String              @id(map: "investigation_alias_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_party_guid String              @db.Uuid
  name                     String              @db.VarChar(512)
  active_ind               Boolean             @default(true)
  create_user_id           String              @db.VarChar(32)
  create_utc_timestamp     DateTime            @default(now()) @db.Timestamp(6)
  update_user_id           String?             @db.VarChar(32)
  update_utc_timestamp     DateTime?           @db.Timestamp(6)
  investigation_party      investigation_party @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_alias_investigation_party_fk")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_alias_h {
  h_investigation_alias_guid    String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business {
  investigation_business_guid        String                               @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  business_guid_ref                  String?                              @db.Uuid
  investigation_party_guid           String?                              @db.Uuid
  name                               String                               @db.VarChar(128)
  active_ind                         Boolean                              @default(true)
  create_user_id                     String                               @db.VarChar(32)
  create_utc_timestamp               DateTime                             @default(now()) @db.Timestamp(6)
  update_user_id                     String?                              @db.VarChar(32)
  update_utc_timestamp               DateTime?                            @db.Timestamp(6)
  investigation_party                investigation_party?                 @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_investigation_party_guid")
  investigation_business_identifier  investigation_business_identifier[]
  investigation_business_person_xref investigation_business_person_xref[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_h {
  h_investigation_business_guid String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_identifier {
  investigation_business_identifier_guid String                 @id(map: "investigation_business_identifier_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_business_guid            String                 @db.Uuid
  business_identifier_code_ref           String                 @db.VarChar(16)
  identifier_value                       String                 @db.VarChar(256)
  active_ind                             Boolean                @default(true)
  create_user_id                         String                 @db.VarChar(32)
  create_utc_timestamp                   DateTime               @default(now()) @db.Timestamp(6)
  update_user_id                         String?                @db.VarChar(32)
  update_utc_timestamp                   DateTime?              @db.Timestamp(6)
  investigation_business                 investigation_business @relation(fields: [investigation_business_guid], references: [investigation_business_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_investigation_business_identifier_business")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_identifier_h {
  h_investigation_business_identifier_guid String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                            String   @db.Uuid
  operation_type                           String   @db.Char(1)
  operation_user_id                        String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                    DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation            Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_contact_method {
  investigation_contact_method_guid String                 @id(map: "investigation_contact_method_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_party_guid          String?                @db.Uuid
  contact_method_type_code_ref      String                 @db.VarChar(10)
  contact_value                     String                 @db.VarChar(512)
  is_primary                        Boolean                @default(false)
  active_ind                        Boolean                @default(true)
  create_user_id                    String                 @db.VarChar(32)
  create_utc_timestamp              DateTime               @default(now()) @db.Timestamp(6)
  update_user_id                    String?                @db.VarChar(32)
  update_utc_timestamp              DateTime?              @db.Timestamp(6)
  investigation_address_guid        String?                @db.Uuid
  investigation_address             investigation_address? @relation(fields: [investigation_address_guid], references: [investigation_address_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_contact_method_address_fk")
  investigation_party               investigation_party?   @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_contact_method_investigation_party_fk")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_contact_method_h {
  h_investigation_contact_method_guid String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                       String   @db.Uuid
  operation_type                      String   @db.Char(1)
  operation_user_id                   String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at               DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation       Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_h {
  h_investigation_guid          String   @id(map: "PK_h_investigation") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_party {
  investigation_party_guid           String                               @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  party_guid_ref                     String?                              @db.Uuid
  party_type_code_ref                String                               @db.VarChar(16)
  investigation_guid                 String                               @db.Uuid
  active_ind                         Boolean                              @default(true)
  create_user_id                     String                               @db.VarChar(32)
  create_utc_timestamp               DateTime                             @db.Timestamp(6)
  update_user_id                     String?                              @db.VarChar(32)
  update_utc_timestamp               DateTime?                            @db.Timestamp(6)
  party_association_role_ref         String?                              @db.VarChar(16)
  created_by_app_user_guid_ref       String?                              @db.Uuid
  placeholder_name                   String?                              @db.VarChar(128)
  placeholder_number                 Int?
  contravention_party_xref           contravention_party_xref[]
  investigation_address              investigation_address[]
  investigation_alias                investigation_alias[]
  investigation_attachment_reference investigation_attachment_reference[]
  investigation_business             investigation_business[]
  investigation_contact_method       investigation_contact_method[]
  investigation                      investigation                        @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_investigation_guid")
  investigation_person               investigation_person[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_party_h {
  h_investigation_party_guid    String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_person {
  investigation_person_guid                       String                                            @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  person_guid_ref                                 String?                                           @db.Uuid
  investigation_party_guid                        String?                                           @db.Uuid
  first_name                                      String?                                           @db.VarChar(128)
  middle_names                                    String?                                           @db.VarChar(256)
  last_name                                       String?                                           @db.VarChar(128)
  active_ind                                      Boolean                                           @default(true)
  create_user_id                                  String                                            @db.VarChar(32)
  create_utc_timestamp                            DateTime                                          @default(now()) @db.Timestamp(6)
  update_user_id                                  String?                                           @db.VarChar(32)
  update_utc_timestamp                            DateTime?                                         @db.Timestamp(6)
  date_of_birth                                   DateTime?                                         @db.Date
  drivers_license_number                          String?                                           @db.VarChar(64)
  approximate_age_code_ref                        String?                                           @db.VarChar(16)
  gender_code_ref                                 String?                                           @db.VarChar(16)
  drivers_license_class                           String?                                           @db.VarChar(128)
  drivers_license_country_code_ref                String?                                           @db.VarChar(4)
  drivers_license_country_subdivision_code_ref    String?                                           @db.VarChar(16)
  height_cm                                       Decimal?                                          @db.Decimal(5, 1)
  weight_kg                                       Decimal?                                          @db.Decimal(5, 1)
  complexion_code_ref                             String?                                           @db.VarChar(16)
  build_code_ref                                  String?                                           @db.VarChar(16)
  hair_colour_code_ref                            String?                                           @db.VarChar(16)
  hair_colour_other                               String?                                           @db.VarChar(128)
  hair_length_code_ref                            String?                                           @db.VarChar(16)
  eye_colour_code_ref                             String?                                           @db.VarChar(16)
  eye_colour_other                                String?                                           @db.VarChar(128)
  facial_hair_ind                                 Boolean?
  additional_hair_descriptors                     String?                                           @db.VarChar(512)
  tattoo_ind                                      Boolean?
  tattoo_description                              String?                                           @db.VarChar(512)
  additional_descriptors                          String?
  comments                                        String?
  bolo_ind                                        Boolean?
  investigation_business_person_xref              investigation_business_person_xref[]
  investigation_party                             investigation_party?                              @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "fk_investigation_party_guid")
  investigation_person_facial_hair_style_code_ref investigation_person_facial_hair_style_code_ref[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_person_h {
  h_investigation_person_guid   String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_status_code {
  investigation_status_code String          @id(map: "PK_ingestigation_status_code") @db.VarChar(10)
  short_description         String          @db.VarChar(50)
  long_description          String?         @db.VarChar(250)
  display_order             Int
  active_ind                Boolean
  create_user_id            String          @db.VarChar(32)
  create_utc_timestamp      DateTime        @db.Timestamp(6)
  update_user_id            String          @db.VarChar(32)
  update_utc_timestamp      DateTime        @db.Timestamp(6)
  investigation             investigation[]

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_status_code_h {
  h_investigation_status_code_guid String   @id(map: "PK_h_investigation_status_code") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                    String   @db.VarChar(20)
  operation_type                   String   @db.Char(1)
  operation_user_id                String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at            DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation    Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model officer_investigation_xref {
  officer_investigation_xref_guid                                                                                             String                          @id(map: "PK_officer_investigation_xref_guid") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  officer_guid_ref                                                                                                            String                          @db.Uuid
  investigation_guid                                                                                                          String                          @db.Uuid
  officer_investigation_xref_code                                                                                             String                          @db.VarChar(10)
  create_user_id                                                                                                              String                          @db.VarChar(32)
  create_utc_timestamp                                                                                                        DateTime                        @db.Timestamp(6)
  update_user_id                                                                                                              String                          @db.VarChar(32)
  update_utc_timestamp                                                                                                        DateTime                        @db.Timestamp(6)
  active_ind                                                                                                                  Boolean
  investigation                                                                                                               investigation                   @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction, map: "FK_officer_investigation_xref__investigation_guid")
  officer_investigation_xref_code_officer_investigation_xref_officer_investigation_xref_codeToofficer_investigation_xref_code officer_investigation_xref_code @relation("officer_investigation_xref_officer_investigation_xref_codeToofficer_investigation_xref_code", fields: [officer_investigation_xref_code], references: [officer_investigation_xref_code], onDelete: NoAction, onUpdate: NoAction, map: "FK_officer_investigation_xref__officer_investigation_xref_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model officer_investigation_xref_code {
  officer_investigation_xref_code                                                                                        String                       @id(map: "PK_officer_investigation_xref_code") @db.VarChar(10)
  short_description                                                                                                      String                       @db.VarChar(50)
  long_description                                                                                                       String?                      @db.VarChar(250)
  display_order                                                                                                          Int
  active_ind                                                                                                             Boolean                      @default(true)
  create_user_id                                                                                                         String                       @db.VarChar(32)
  create_utc_timestamp                                                                                                   DateTime                     @db.Timestamp(6)
  update_user_id                                                                                                         String                       @db.VarChar(32)
  update_utc_timestamp                                                                                                   DateTime                     @db.Timestamp(6)
  officer_investigation_xref_officer_investigation_xref_officer_investigation_xref_codeToofficer_investigation_xref_code officer_investigation_xref[] @relation("officer_investigation_xref_officer_investigation_xref_codeToofficer_investigation_xref_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model officer_investigation_xref_code_h {
  h_officer_investigation_xref_code_guid String   @id(map: "PK_h_officer_investigation_xref_code") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                          String   @db.VarChar(20)
  operation_type                         String   @db.Char(1)
  operation_user_id                      String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                  DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation          Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model officer_investigation_xref_h {
  h_officer_investigation_xref_guid String   @id(map: "PK_h_officer_investigation_xref") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                     String   @db.Uuid
  operation_type                    String   @db.Char(1)
  operation_user_id                 String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at             DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation     Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model task {
  task_guid                                                                     String                  @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_guid                                                            String                  @db.Uuid
  task_type_code                                                                String?                 @db.VarChar(16)
  task_status_code                                                              String                  @db.VarChar(16)
  task_number                                                                   Int
  app_create_user_guid_ref                                                      String                  @db.Uuid
  app_create_utc_timestamp                                                      DateTime                @db.Timestamp(6)
  app_update_user_guid_ref                                                      String?                 @db.Uuid
  app_update_utc_timestamp                                                      DateTime?               @db.Timestamp(6)
  assigned_app_user_guid_ref                                                    String?                 @db.Uuid
  description                                                                   String?
  active_ind                                                                    Boolean                 @default(true)
  create_user_id                                                                String                  @db.VarChar(32)
  create_utc_timestamp                                                          DateTime                @default(now()) @db.Timestamp(6)
  update_user_id                                                                String?                 @db.VarChar(32)
  update_utc_timestamp                                                          DateTime?               @db.Timestamp(6)
  task_category_type_code                                                       String                  @db.VarChar(16)
  remarks                                                                       String                  @default("") @db.VarChar(256)
  due_date                                                                      DateTime                @default(dbgenerated("CURRENT_DATE")) @db.Date
  activity_note                                                                 activity_note[]
  diary_date                                                                    diary_date[]
  exhibit                                                                       exhibit[]
  investigation                                                                 investigation           @relation(fields: [investigation_guid], references: [investigation_guid], onDelete: NoAction, onUpdate: NoAction)
  task_category_type_code_task_task_category_type_codeTotask_category_type_code task_category_type_code @relation("task_task_category_type_codeTotask_category_type_code", fields: [task_category_type_code], references: [task_category_type_code], onDelete: NoAction, onUpdate: NoAction)
  task_status_code_task_task_status_codeTotask_status_code                      task_status_code        @relation("task_task_status_codeTotask_status_code", fields: [task_status_code], references: [task_status_code], onDelete: NoAction, onUpdate: NoAction)
  task_type_code_task_task_type_codeTotask_type_code                            task_type_code?         @relation("task_task_type_codeTotask_type_code", fields: [task_type_code], references: [task_type_code], onDelete: NoAction, onUpdate: NoAction)

  @@unique([investigation_guid, task_number], map: "unique_task_number_per_investigation")
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model task_category_type_code {
  task_category_type_code                                                        String           @id @db.VarChar(16)
  short_description                                                              String           @db.VarChar(64)
  long_description                                                               String?          @db.VarChar(256)
  display_order                                                                  Int?
  active_ind                                                                     Boolean          @default(true)
  create_user_id                                                                 String           @db.VarChar(32)
  create_utc_timestamp                                                           DateTime         @default(now()) @db.Timestamp(6)
  update_user_id                                                                 String?          @db.VarChar(32)
  update_utc_timestamp                                                           DateTime?        @db.Timestamp(6)
  task_task_task_category_type_codeTotask_category_type_code                     task[]           @relation("task_task_category_type_codeTotask_category_type_code")
  task_type_code_task_type_code_task_category_type_codeTotask_category_type_code task_type_code[] @relation("task_type_code_task_category_type_codeTotask_category_type_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model task_h {
  h_task_guid                   String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model task_status_code {
  task_status_code                             String    @id @db.VarChar(16)
  short_description                            String    @db.VarChar(64)
  long_description                             String?   @db.VarChar(256)
  display_order                                Int?
  active_ind                                   Boolean   @default(true)
  create_user_id                               String    @db.VarChar(32)
  create_utc_timestamp                         DateTime  @default(now()) @db.Timestamp(6)
  update_user_id                               String?   @db.VarChar(32)
  update_utc_timestamp                         DateTime? @db.Timestamp(6)
  task_task_task_status_codeTotask_status_code task[]    @relation("task_task_status_codeTotask_status_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model task_type_code {
  task_type_code                                                                          String                  @id @db.VarChar(16)
  task_category_type_code                                                                 String                  @db.VarChar(16)
  short_description                                                                       String                  @db.VarChar(64)
  long_description                                                                        String?                 @db.VarChar(256)
  display_order                                                                           Int?
  active_ind                                                                              Boolean                 @default(true)
  create_user_id                                                                          String                  @db.VarChar(32)
  create_utc_timestamp                                                                    DateTime                @default(now()) @db.Timestamp(6)
  update_user_id                                                                          String?                 @db.VarChar(32)
  update_utc_timestamp                                                                    DateTime?               @db.Timestamp(6)
  task_task_task_type_codeTotask_type_code                                                task[]                  @relation("task_task_type_codeTotask_type_code")
  task_category_type_code_task_type_code_task_category_type_codeTotask_category_type_code task_category_type_code @relation("task_type_code_task_category_type_codeTotask_category_type_code", fields: [task_category_type_code], references: [task_category_type_code], onDelete: NoAction, onUpdate: NoAction)

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ticket {
  ticket_guid                                                         String              @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  enforcement_action_guid                                             String              @db.Uuid
  ticket_outcome_code                                                 String              @db.VarChar(16)
  ticket_amount                                                       Decimal             @db.Decimal(10, 2)
  ticket_number                                                       String              @db.VarChar(32)
  paid_date                                                           DateTime?           @db.Date
  active_ind                                                          Boolean             @default(true)
  create_user_id                                                      String              @db.VarChar(32)
  create_utc_timestamp                                                DateTime            @default(now()) @db.Timestamp(6)
  update_user_id                                                      String?             @db.VarChar(32)
  update_utc_timestamp                                                DateTime?           @db.Timestamp(6)
  enforcement_action                                                  enforcement_action  @relation(fields: [enforcement_action_guid], references: [enforcement_action_guid], onDelete: NoAction, onUpdate: NoAction)
  ticket_outcome_code_ticket_ticket_outcome_codeToticket_outcome_code ticket_outcome_code @relation("ticket_ticket_outcome_codeToticket_outcome_code", fields: [ticket_outcome_code], references: [ticket_outcome_code], onDelete: NoAction, onUpdate: NoAction)

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ticket_h {
  h_ticket_guid                 String   @id @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ticket_outcome_code {
  ticket_outcome_code                                    String    @id @db.VarChar(16)
  short_description                                      String    @db.VarChar(64)
  long_description                                       String?   @db.VarChar(256)
  display_order                                          Int?
  active_ind                                             Boolean   @default(true)
  create_user_id                                         String    @db.VarChar(32)
  create_utc_timestamp                                   DateTime  @default(now()) @db.Timestamp(6)
  update_user_id                                         String?   @db.VarChar(32)
  update_utc_timestamp                                   DateTime? @db.Timestamp(6)
  ticket_ticket_ticket_outcome_codeToticket_outcome_code ticket[]  @relation("ticket_ticket_outcome_codeToticket_outcome_code")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_address {
  investigation_address_guid                 String                                       @id(map: "investigation_address_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_party_guid                   String                                       @db.Uuid
  address_name                               String                                       @db.VarChar(128)
  address                                    String?                                      @db.VarChar(120)
  city                                       String?                                      @db.VarChar(128)
  country_subdivision_code_ref               String?                                      @db.VarChar(16)
  postal_code                                String?                                      @db.VarChar(16)
  country_code_ref                           String?                                      @db.VarChar(4)
  is_primary                                 Boolean                                      @default(false)
  active_ind                                 Boolean                                      @default(true)
  create_user_id                             String                                       @db.VarChar(32)
  create_utc_timestamp                       DateTime                                     @default(now()) @db.Timestamp(6)
  update_user_id                             String?                                      @db.VarChar(32)
  update_utc_timestamp                       DateTime?                                    @db.Timestamp(6)
  display_in_investigation_ind               Boolean                                      @default(true)
  investigation_party                        investigation_party                          @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_address_party_fk")
  investigation_business_person_address_xref investigation_business_person_address_xref[]
  investigation_contact_method               investigation_contact_method[]

  @@schema("investigation")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_address_h {
  h_investigation_address_guid  String   @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@ignore
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_person_facial_hair_style_code_ref {
  investigation_person_facial_hair_style_code_ref_guid String               @id(map: "investigation_person_facial_hair_style_code_ref_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_person_guid                            String               @db.Uuid
  facial_hair_style_code_ref                           String               @db.VarChar(16)
  active_ind                                           Boolean              @default(true)
  create_user_id                                       String               @db.VarChar(32)
  create_utc_timestamp                                 DateTime             @default(now()) @db.Timestamp(6)
  update_user_id                                       String?              @db.VarChar(32)
  update_utc_timestamp                                 DateTime?            @db.Timestamp(6)
  investigation_person                                 investigation_person @relation(fields: [investigation_person_guid], references: [investigation_person_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_person_facial_hair_style_code_ref_investigation_p")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_person_facial_hair_style_code_ref_h {
  h_investigation_person_facial_hair_style_code_ref_guid String   @id(map: "pk_h_investigation_person_facial_hair_style_code_ref") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                                          String   @db.Uuid
  operation_type                                         String   @db.Char(1)
  operation_user_id                                      String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                                  DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation                          Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_attachment_reference {
  investigation_attachment_reference_guid String              @id(map: "investigation_attachment_reference_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_party_guid                String              @db.Uuid
  object_guid_ref                         String              @db.Uuid
  s3_version_ref                          String              @db.VarChar(512)
  filename_text                           String              @db.VarChar(512)
  coms_created_date                       DateTime            @db.Timestamp(6)
  thumb_object_guid_ref                   String?             @db.Uuid
  thumb_s3_version_ref                    String?             @db.VarChar(512)
  active_ind                              Boolean             @default(true)
  create_user_id                          String              @db.VarChar(32)
  create_utc_timestamp                    DateTime            @default(now()) @db.Timestamp(6)
  update_user_id                          String?             @db.VarChar(32)
  update_utc_timestamp                    DateTime?           @db.Timestamp(6)
  investigation_party                     investigation_party @relation(fields: [investigation_party_guid], references: [investigation_party_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_attachment_reference_investigation_party_fk")

  @@schema("investigation")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_attachment_reference_h {
  h_investigation_attachment_reference_guid String   @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                             String   @db.Uuid
  operation_type                            String   @db.Char(1)
  operation_user_id                         String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                     DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation             Json?

  @@ignore
  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_person_address_xref {
  investigation_business_person_address_xref_guid String                             @id(map: "investigation_business_person_address_xref_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_business_person_xref_guid         String                             @db.Uuid
  investigation_address_guid                      String                             @db.Uuid
  active_ind                                      Boolean                            @default(true)
  create_user_id                                  String                             @db.VarChar(32)
  create_utc_timestamp                            DateTime                           @default(now()) @db.Timestamp(6)
  update_user_id                                  String?                            @db.VarChar(32)
  update_utc_timestamp                            DateTime?                          @db.Timestamp(6)
  investigation_address                           investigation_address              @relation(fields: [investigation_address_guid], references: [investigation_address_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_business_person_address_xref_address_fk")
  investigation_business_person_xref              investigation_business_person_xref @relation(fields: [investigation_business_person_xref_guid], references: [investigation_business_person_xref_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_business_person_address_xref_bp_xref_fk")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_person_address_xref_h {
  h_investigation_business_person_address_xref_guid String   @id(map: "pk_h_investigation_business_person_address_xref") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                                     String   @db.Uuid
  operation_type                                    String   @db.Char(1)
  operation_user_id                                 String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                             DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation                     Json?

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_person_xref {
  investigation_business_person_xref_guid    String                                       @id(map: "investigation_business_person_xref_pk") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  investigation_business_guid                String                                       @db.Uuid
  investigation_person_guid                  String                                       @db.Uuid
  business_person_xref_code_ref              String                                       @db.VarChar(16)
  active_ind                                 Boolean                                      @default(true)
  create_user_id                             String                                       @db.VarChar(32)
  create_utc_timestamp                       DateTime                                     @default(now()) @db.Timestamp(6)
  update_user_id                             String?                                      @db.VarChar(32)
  update_utc_timestamp                       DateTime?                                    @db.Timestamp(6)
  display_in_investigation_ind               Boolean                                      @default(true)
  title_role                                 String?                                      @db.VarChar(256)
  is_primary                                 Boolean                                      @default(false)
  investigation_business_person_address_xref investigation_business_person_address_xref[]
  investigation_business                     investigation_business                       @relation(fields: [investigation_business_guid], references: [investigation_business_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_business_person_xref_business_fk")
  investigation_person                       investigation_person                         @relation(fields: [investigation_person_guid], references: [investigation_person_guid], onDelete: NoAction, onUpdate: NoAction, map: "investigation_business_person_xref_person_fk")

  @@schema("investigation")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model investigation_business_person_xref_h {
  h_investigation_business_person_xref_guid String   @id(map: "pk_h_investigation_business_person_xref") @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                             String   @db.Uuid
  operation_type                            String   @db.Char(1)
  operation_user_id                         String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at                     DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation             Json?

  @@schema("investigation")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model exhibit_h {
  h_exhibit_guid                String   @default(dbgenerated("public.uuid_generate_v4()")) @db.Uuid
  target_row_id                 String   @db.Uuid
  operation_type                String   @db.Char(1)
  operation_user_id             String   @default(dbgenerated("CURRENT_USER")) @db.VarChar(32)
  operation_executed_at         DateTime @default(now()) @db.Timestamp(6)
  data_after_executed_operation Json?

  @@ignore
  @@schema("investigation")
}
