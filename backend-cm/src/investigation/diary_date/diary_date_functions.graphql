type Query {
  diaryDates(investigationGuid: String!): [DiaryDate!]
  diaryDatesByTask(taskGuid: String!): [DiaryDate!]
}

type Mutation {
  saveDiaryDate(input: DiaryDateInput!): DiaryDate!
  deleteDiaryDate(diaryDateGuid: String!): Boolean!
}
