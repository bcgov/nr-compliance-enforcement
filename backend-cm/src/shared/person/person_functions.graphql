type Query {
  people: [Person]
  person(personGuid: String!): Person
}

type Mutation {
  createPerson(input: PersonInput!): Person!
  updatePerson(personGuid: String!, input: PersonInput!): Person!
  deletePerson(personGuid: String!): Person!
}
