input BusinessIdentifierInput {
  businessGuid: String
  identifierValue: String!
  identifierCode: String!
}

input BusinessIdentifierUpdateInput {
  businessGuid: String
  businessIdentifierGuid: String
  identifierValue: String!
  identifierCode: String!
}

# Distinct input type as shared parties currently have mandatory fields.  Could combine in future.
input BusinessIdentifierMatchInput {
  identifierCode: String!
  identifierValue: String
}
