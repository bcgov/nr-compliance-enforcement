type IPMAuthCategoryCodeType {
  ipmAuthCategoryCode: String!
  shortDescription: String
  longDescription: String
  displayOrder: Int
  activeIndicator: Boolean
}

type Query {
  ipmAuthCategoryCodes: [IPMAuthCategoryCodeType]!
}
