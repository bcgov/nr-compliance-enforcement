import test from "@playwright/test";
import COMPLAINT_TYPES from "../../src/app/types/app/complaint-types";

test.describe("Complaint Assign and Status Popover spec", () => {
  const complaintTypes = ["#hwcr-tab", "#ers-tab", "#gir-tab"];
});
